#include <bits/stdc++.h>
#define forLoop(len) for(int i = 0; i < len; i++)
#define mk make_pair
#define pb push_back

using namespace std;

int main() {
    int l, r, c;

    while(cin >> l >> r >> c && (l+r+c) != 0) {
        forLoop(l) {
            vector<vector<char>> lvl;

        }
    }

    return 0;
}
