#include <bits/stdc++.h>

using namespace std;

int A(vector<vector<int>> g) {
	
}

int main() {
	int m, n;
	cin >> m >> n;
	
	vector<vector<int>> vvi (m, vector<int> (n));

	for(int i = 0; i < m; i++) {
		for(int j = 0; j < n; j++) {
			cin >> vii[m][n];
		}
	}

	int dist = A(vii);

	return 0;
}
