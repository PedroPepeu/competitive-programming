#include<bits/stdc++.h>

using namespace std;

typedef long long int lli;

int main() {
    lli n;
    while(cin >> n && n != 0) {
        vector<pair<lli, int> v;
        lli ex;
        cin >> ex;
        v.push_back(make_pair(ex, 0));
        

        for(int i = 1; i < n; i++) {
             
        }
    }

    return 0;
}
