#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int a, b, v, A, s;
    while(cin >> a >> b >> v >> A >> s && (a+b+v+A+s) != 0) {
        int dist = v*s;
        double x0=a/2, y0=b/2;
        while(dist>0) {
            x0 = ;
        }
    }
    return 0;
}
