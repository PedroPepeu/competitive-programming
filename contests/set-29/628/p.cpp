#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
    int n, m;
    vector<int> words;

    while(cin >> n) {
        cin >> n;
        while(n--) {
            string s;
            cin >> s;
            words.push_back(s);
        }

        cin >> m;
        string r;
        vector<string> rules;
        while(m--) {
            cin >> r;
            rules.push_back(r);
        }

        string a[pow(words.size(), )]
        
    }
}
