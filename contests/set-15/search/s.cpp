#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<string> wTi;
vector<string> ph;

void titleFound(string s) {
    char c;
    for(auto &x : s) {
        c = tolower(x);

    }
}

int main() {
    string str;
    while(cin >> str && str != "::") {
        wTi.push_back(str);
    }

    while(getline(cin , str) && str != "") {
        
    }    
}
