#include <bits/stdc++.h>

using namespace std;

int main() {
	int a, b, chain = 0;
	while(scanf("%d %d", &a, &b) && (a != 0 && b != 0)) {
		// k are the depots
		// n is the restaurants
		vector<int> dist (a);
		for(int i = 0; i < n; i++) {
			scanf("%d", &dist[i]);
		}

		chain+=1;
		printf("Chain %d\n", chain);
		for(int i = 0; i < n; i++) {
			
		}
	}
	return 0;
}
