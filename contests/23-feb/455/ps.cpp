#include <bits/stdc++.h>

using namespace std;

int countPeriods(string s, int i) {
    for(string::iterator it = s.begin(); it != s.end(); it++) {
        
    }
}

int main() {
    int t;
    while(t--) {
        string s;
        cin >> s;

        cout << ans(s, 1) << endl;
    }
    return 0;
}
