#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, c;
    while(cin >> n && n != 0) {
        vector<int> nums;
        for(int i = 0; i < n; i++) {
            cin >> c;
            nums.push_back(c);
        }

    }
}
