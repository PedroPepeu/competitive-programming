#include<bits/stdc++.h>

#define pb push_back

using namespace std;

int main() {
	int t;
	cin >> t;
	while(t--) {
		int sizeArr;
		cin >> sizeArr;
		
		int init[sizeArr];
		for(int i = 0; i < sizeArr; i++) {
			cin >> init[i];
		}

		while(va.size() > 1) {
			for(int i = 0; i < sz; i++) {
				
			}

		}
	}
	return 0;
}
