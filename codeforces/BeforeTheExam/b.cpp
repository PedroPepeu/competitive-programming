#include <bits/stdc++.h>
#define forLoop(len) for(int i = 0; i < len; i++)

using namespace std;

int main() {
    int d, sT, mT, mat;
    cin >> d >> sT;
    vector<pair<int, int>> v (d);
    forLoop(d) cin >> v[i].first >> v[i].second;



    return 0;
}
